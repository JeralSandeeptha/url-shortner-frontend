export type TestimonialAuthCardProps = {
  id: string;
  avatar: string;
  name: string;
  username: string;
  testimonial: string;
};

export type HeaderProps = {
  title: string;
  className?: string;
};

export type DashboardHeaderProps = {
  title: string;
  isSubHeader?: boolean;
  subTitle?: string;
};
