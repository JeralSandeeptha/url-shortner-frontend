export type TestimonialAuthCardProps = {
  id: string;
  avatar: string;
  name: string;
  username: string;
  testimonial: string;
};

export type HeaderProps = {
  title: string;
  className?: string;
};
